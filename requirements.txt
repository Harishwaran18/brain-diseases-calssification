# NeuroCure — cloud deployment requirements
# Install with: pip install -r requirements.txt
#
# NOTE on PyTorch: Streamlit Community Cloud and Render provide CPU-only
# instances. The CPU wheel index below avoids downloading multi-GB CUDA
# wheels. If you deploy on a GPU machine, replace the torch line with a plain
#   torch>=2.1
# and install the appropriate CUDA wheels from https://pytorch.org
--extra-index-url https://download.pytorch.org/whl/cpu

# Core scientific stack
numpy>=1.24,<3.0
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
pandas>=2.0
nibabel>=5.1
pyyaml>=6.0
tqdm>=4.65

# Imaging / mesh
opencv-python-headless>=4.8
trimesh>=4.0
fast-simplification>=0.1.7
matplotlib>=3.7
plotly>=5.18

# Deep learning (CPU build)
torch>=2.1
monai>=1.3

# Real brain data + atlas fetching
nilearn>=0.10

# Streamlit app framework
streamlit>=1.30
